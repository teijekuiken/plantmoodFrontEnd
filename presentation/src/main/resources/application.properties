# DATABASE CONNECTION
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/${MYSQL_DATABASE:central_system}
spring.datasource.username=${MYSQL_USER:plantmood}
spring.datasource.password=${MYSQL_PASSWORD:plantmood}
spring.jpa.open-in-view=true
